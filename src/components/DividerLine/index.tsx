import { View } from 'react-native';

const DividerLine = () => {
    return (
        <View
            style={{
                backgroundColor: '#dbdbdb',
                height: 2,
                marginBottom: 120,
                width: '100%',
            }}
        />
    );
};

export default DividerLine;
